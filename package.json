{
  "name": "arc-raiders",
  "version": "1.0.0",
  "description": "Interactive web tools for Arc Raiders players to search items, plan crafting, and visualize quest chains.",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
